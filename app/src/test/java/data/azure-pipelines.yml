trigger:
  branches:
      include:
        - main
  paths:
      include:
        - app/src/test/java/data/*

resources:
  repositories:
    - repository: templates
      type: git
      name: util_pipeline

extends:
  template: QA/karate-backend/upload-data.yml@templates