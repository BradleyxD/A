trigger:
  branches:
      include:
        - main
  paths:
      include:
        - app/*
      exclude:
        - app/src/test/java/data/*

resources:
  repositories:
    - repository: templates
      type: git
      name: util_pipeline

extends:
  template: QA/karate-backend/src-karate.yml@templates